fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
python-dotenv>=1.0,<2.0
# Used by the style-refs agent to download user-uploaded reference
# images (e.g. Supabase public URLs) for VLM analysis. FastAPI already
# pulls httpx in transitively, but pin explicitly so style_refs can
# rely on it.
httpx>=0.27,<1.0
# LLM client. Works with any OpenAI-compatible endpoint
# (OpenRouter, Together, Groq, Fireworks, vLLM, Ollama, OpenAI).
openai>=1.40,<2.0
# -------------------------------------------------------------------------
# Dev / test (optional at runtime; install when running pytest).
# -------------------------------------------------------------------------
pytest>=8.0,<9.0

# -------------------------------------------------------------------------
# Optional: Apple FastVLM (on-device VLM via MLX, Apple Silicon only).
# Required only when GENESIS_VLM_BACKEND=fastvlm. Leave commented for
# Linux/Windows hosts -- the OpenAI-compat backend works everywhere.
# -------------------------------------------------------------------------
# mlx>=0.18
# mlx-vlm>=0.1.6
